# Fizetési Info Platform - Environment Variables Template
# Másold át ezt a fájlt .env néven és töltsd ki a saját értékeiddel

# ============================================
# APPLICATION SETTINGS
# ============================================
APP_NAME=Fizetesi_Info_Platform
APP_ENV=development
DEBUG=True
SECRET_KEY=change-this-secret-key-in-production-use-openssl-rand-hex-32

# ============================================
# API CONFIGURATION
# ============================================
API_HOST=0.0.0.0
API_PORT=8000
API_PREFIX=/api
# CORS_ORIGINS can be specified in multiple formats:
# - JSON array: ["http://localhost:3000","http://localhost:8000"]
# - Comma-separated: http://localhost:3000,http://localhost:8000
# - Single URL: http://localhost:3000
# - Empty (uses default): 
CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Helyi fejlesztéshez
DATABASE_URL=postgresql://admin:password@localhost:5432/fizetesek
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=fizetesek
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Railway Production (automatikusan beállítva Railway által)
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# POSTGRES_USER=${{Postgres.PGUSER}}
# POSTGRES_PASSWORD=${{Postgres.PGPASSWORD}}
# POSTGRES_DB=${{Postgres.PGDATABASE}}
# POSTGRES_HOST=${{Postgres.PGHOST}}
# POSTGRES_PORT=${{Postgres.PGPORT}}

# ============================================
# REDIS CONFIGURATION
# ============================================
# Helyi fejlesztéshez
REDIS_URL=redis://localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379

# Railway Production (automatikusan beállítva Railway által)
# REDIS_URL=${{Redis.REDIS_URL}}
# REDIS_HOST=${{Redis.REDIS_HOST}}
# REDIS_PORT=${{Redis.REDIS_PORT}}

# ============================================
# JWT AUTHENTICATION
# ============================================
JWT_SECRET_KEY=your-jwt-secret-key-use-openssl-rand-hex-32
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# OPENAI CONFIGURATION
# ============================================
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_MAX_TOKENS=2000
OPENAI_TEMPERATURE=0.7

# ============================================
# SCRAPING CONFIGURATION
# ============================================
SCRAPE_INTERVAL=3600
MAX_PAGES_PER_SITE=10
REQUEST_DELAY_MIN=1
REQUEST_DELAY_MAX=3
USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# ============================================
# PROXY SETTINGS (Optional)
# ============================================
USE_PROXY=False
PROXY_URL=
PROXY_USERNAME=
PROXY_PASSWORD=

# ============================================
# RATE LIMITING
# ============================================
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# ============================================
# CELERY CONFIGURATION
# ============================================
# Helyi fejlesztéshez
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
CELERY_TASK_SERIALIZER=json
CELERY_RESULT_SERIALIZER=json
CELERY_TIMEZONE=Europe/Budapest

# Railway Production (automatikusan beállítva Railway által)
# CELERY_BROKER_URL=${{Redis.REDIS_URL}}
# CELERY_RESULT_BACKEND=${{Redis.REDIS_URL}}

# ============================================
# MONITORING & LOGGING
# ============================================
SENTRY_DSN=
ENABLE_SENTRY=False
LOG_LEVEL=INFO
LOG_FILE=app.log

# ============================================
# FRONTEND CONFIGURATION (Vercel)
# ============================================
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=Fizetesi_Info_Platform
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_ENABLE_ANALYTICS=false
NEXT_PUBLIC_SENTRY_DSN=

# Production (Vercel)
# NEXT_PUBLIC_API_URL=https://your-backend.railway.app
# NEXT_PUBLIC_APP_ENV=production
# NEXT_PUBLIC_ENABLE_ANALYTICS=true

# ============================================
# DEPLOYMENT NOTES
# ============================================
# 
# RAILWAY DEPLOYMENT:
# -------------------
# 1. PostgreSQL és Redis service hozzáadása a Railway projekthez
# 2. DATABASE_URL és REDIS_URL automatikusan beállításra kerül
# 3. Csak a következő változókat kell manuálisan beállítani:
#    - SECRET_KEY (generálás: openssl rand -hex 32)
#    - JWT_SECRET_KEY (generálás: openssl rand -hex 32)
#    - OPENAI_API_KEY (OpenAI API kulcs)
#    - CORS_ORIGINS (frontend domain-nel)
#
# VERCEL DEPLOYMENT:
# ------------------
# 1. NEXT_PUBLIC_API_URL beállítása Railway backend URL-re
# 2. További environment változók a Vercel Dashboard-on
#
